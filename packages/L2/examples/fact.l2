(l2 (n)
    (let ((fact (allocate 1)))
        (begin
            (store fact 0)

            (if
                (== n 0)
            1
            (* n
                (fact (- n 1)))))
        )
        (load fact 0))