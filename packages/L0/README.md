Within syntax.py l0

We have Nat back same as it was in l2 and l3 

Program now has a tag of "l0" and a sequence of procedures. It now lacks a body using the sequence of procedures to act as a body

Procedure is a class that has an identifiable name and parameters of identifiers. Its body is made up of statements making up for the lack of a body in Program. It's, in this way, a collection of the various functions in the program.

Statement now has the new classes of Address and Call while lacking the Abstract and Apply of L1

Copy, Immediate, Primitive, Branch, and Halt are the same as in L1

Allocate count is now a Nat creating a slight degree of indirectness in its function
Load and Stores index is now a Nat

Address gets the address of a named procedure. Destination being where to store this address and name being which one we are getting.

Call calls a procedure in memory using a refernece to it. It lacks a then as it is performed at the end of the ongoing procedure